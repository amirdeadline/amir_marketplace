---
name: amir-ssh:session
description: Open interactive ssh (human-driven).
---

# /amir-ssh:session

`python "${CLAUDE_PLUGIN_ROOT}/scripts/ssh_cli.py" session -- $ARGUMENTS`
Claude must not drive interactive session blind.
