---
name: project_cost
description: "Aggregate and present cost telemetry from activity events. All USD figures are estimates unless explicitly logged."
---

# project_cost

Follow the procedure in the host-agnostic amir skill file `skill-specs/project_cost.md`.

**Resolve path:** `<amir-root>/skill-specs/project_cost.md` where `<amir-root>` is this plugin directory (contains `core/`, `skill-specs/`, and `tools/`).

1. Read the skill file completely.
2. Execute per its **Behavior** section.
3. Use `node tools/*.js` from the amir `tools/` directory.
4. Follow `core/` modules referenced by the skill — never restate them inline.
