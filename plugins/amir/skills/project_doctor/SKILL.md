---
name: project_doctor
description: "Run structural and operational health checks; present findings as a severity table with suggested fixes."
---

# project_doctor

Follow the procedure in the host-agnostic amir skill file `skill-specs/project_doctor.md`.

**Resolve path:** `<amir-root>/skill-specs/project_doctor.md` where `<amir-root>` is this plugin directory (contains `core/`, `skill-specs/`, and `tools/`).

1. Read the skill file completely.
2. Execute per its **Behavior** section.
3. Use `node tools/*.js` from the amir `tools/` directory.
4. Follow `core/` modules referenced by the skill — never restate them inline.
