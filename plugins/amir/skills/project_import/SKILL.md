---
name: project_import
description: "Wrap an **existing** repository or codebase as an amir project. Review what already exists before discovery; create amir structure adjacent to or inside the repo per human choice. Distinct from greenfield `/project_create`."
---

# project_import

Follow the procedure in the host-agnostic amir skill file `skill-specs/project_import.md`.

**Resolve path:** `<amir-root>/skill-specs/project_import.md` where `<amir-root>` is this plugin directory (contains `core/`, `skill-specs/`, and `tools/`).

1. Read the skill file completely.
2. Execute per its **Behavior** section.
3. Use `node tools/*.js` from the amir `tools/` directory.
4. Follow `core/` modules referenced by the skill — never restate them inline.
