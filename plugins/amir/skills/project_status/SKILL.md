---
name: project_status
description: "Render and present the project status dashboard. Read-only visibility into phase, progress, pending approvals, and current task."
---

# project_status

Follow the procedure in the host-agnostic amir skill file `skill-specs/project_status.md`.

**Resolve path:** `<amir-root>/skill-specs/project_status.md` where `<amir-root>` is this plugin directory (contains `core/`, `skill-specs/`, and `tools/`).

1. Read the skill file completely.
2. Execute per its **Behavior** section.
3. Use `node tools/*.js` from the amir `tools/` directory.
4. Follow `core/` modules referenced by the skill — never restate them inline.
