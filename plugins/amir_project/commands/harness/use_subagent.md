---
description: DEPRECATED alias
---

# /amir:use_subagent (DEPRECATED)

This command is a deprecated alias. Do the following, in order:

1. Print this warning to the user verbatim:

   > WARNING: `/amir:use_subagent` is deprecated and will be removed in amir_project 1.0.
   > Use `/amir:use_subagents` instead (provided by the amir_system plugin).

2. Then invoke the replacement: run `/amir:use_subagents` with the same arguments
   (`$ARGUMENTS`) and continue there. Do not perform any other work under the
   old name.
