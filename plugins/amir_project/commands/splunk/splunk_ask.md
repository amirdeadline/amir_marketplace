---
description: SPL / Splunk how-to via spl skill.
---

> **Component gate (amir_project):** before doing anything else, read `.amir/project.yaml` at the project root and verify that `plugins.amir_project.components` includes `"splunk"`. If the manifest is missing or `"splunk"` is not listed, STOP — do not execute this command — and tell the user to enable the `splunk` component via `/amir:configure_project`.

# /amir:splunk_ask

`$ARGUMENTS`
