---
name: git_push
description: "Confirm remote and branch, show what will push, execute push, and report result with evidence."
---

# git_push

Follow the procedure in the host-agnostic amir skill file `skill-specs/git_push.md`.

**Resolve path:** `<amir-root>/skill-specs/git_push.md` where `<amir-root>` is this plugin directory (contains `core/`, `skill-specs/`, and `tools/`).

1. Read the skill file completely.
2. Execute per its **Behavior** section.
3. Use `node tools/*.js` from the amir `tools/` directory.
4. Follow `core/` modules referenced by the skill — never restate them inline.
