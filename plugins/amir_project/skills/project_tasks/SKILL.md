---
name: project_tasks
description: "Render and present the task board view. Read-only visibility into active and finished tasks."
---

# project_tasks

Follow the procedure in the host-agnostic amir skill file `skill-specs/project_tasks.md`.

**Resolve path:** `<amir-root>/skill-specs/project_tasks.md` where `<amir-root>` is this plugin directory (contains `core/`, `skill-specs/`, and `tools/`).

1. Read the skill file completely.
2. Execute per its **Behavior** section.
3. Use `node tools/*.js` from the amir `tools/` directory.
4. Follow `core/` modules referenced by the skill — never restate them inline.
