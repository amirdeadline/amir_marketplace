# Step <N>: <title>

## Purpose

<what this step learns and how it narrows the problem>

## Current Hypothesis

<one focused hypothesis, or "baseline discovery">

## Why More Evidence Is Needed

<only for Step 2+; omit on Step 1 if N/A>

## Read-Only Commands

1. `<exact command>`
   - Purpose:
   - Expected evidence: *(Step 1)* / What would support vs reject: *(Step 2+)*
   - Safety classification: Read-only

2. `<exact command>`
   - Purpose:
   - Expected evidence / support vs reject:
   - Safety classification: Read-only

## Classifier note

Every command above was classified `READ_ONLY` per `templates/troubleshoot/command-classifier.md`. Any `UNCERTAIN` or `STATE_CHANGING` command was excluded from this step.
