"""amir deterministic tooling package (Python 3.12, stdlib + pyyaml + jsonschema).

Modules are also runnable as flat scripts (amirctl.py bootstraps sys.path), so the package
works both installed as a plugin and executed from an arbitrary checkout.
"""
